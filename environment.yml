name: who_covers
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - cfbd
      - pandas
      - numpy
      - matplotlib
      - scikit-learn
      - jupyter
      - notebook
